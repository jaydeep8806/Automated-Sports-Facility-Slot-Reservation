services:
  - type: web
    name: sportslot
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://sportslot-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
